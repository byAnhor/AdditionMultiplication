<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.byanhor.romane.tabledadditions">

    <supports-screens android:resizeable="false"
    android:smallScreens="True"
    android:normalScreens="True"
    android:largeScreens="True"
    android:xlargeScreens="True"
    android:anyDensity="True"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/waiting"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/waiting"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:minSdkVersion="23"
        android:versionCode="3"
        android:versionName="1.2">
        <activity android:name=".ChoixTables">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TableDesX"></activity>
        <activity android:name=".UserStatsDisplay"></activity>
    </application>

</manifest>